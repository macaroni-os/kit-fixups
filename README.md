# M.A.R.K. Kit-Fixups Repository

The `MARK-I` release is the stable
release for *Desktop* x86-64.

This branch contains the YAML files used by `mark-devkit` to
merge kits in the `mark-i` branch of the defined kits from `mark-iii`.

