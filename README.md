# M.A.R.K. Kit-Fixups Repository

The `MARK-V` release also knows as the Suitcase Suit is the stable
release of Mark for *server* and *container*.

This branch contains the YAML files used by `mark-devkit` to
merge kits in the `mark-v` branch of the defined kits.

