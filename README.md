# M.A.R.K. Kit-Fixups Repository

The `MARK-UNSTABLE` release is the world of the chaos. We can
push PR to upgrade and break everything. But better in an organized mode
if it's possible :)

This branch contains the YAML files used by `mark-devkit` to
autogen kits in the `mark-unstable` branch of the defined kits.

