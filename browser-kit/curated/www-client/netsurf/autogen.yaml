netsurf_rules:
  defaults:
    url_suffix: libs/releases
  packages:
    - netsurf:
        cat: www-client
        url_suffix: netsurf/releases/source-full
        revision:
          # version number is a str to preserve trailing zeros (https://bugs.funtoo.org/browse/FL-9547)
          '3.10': 4
