libplist:
  generator: github-1
  packages:
    - libplist:
        github:
          user: libimobiledevice
          repo: libplist
          query: releases
          tarball: libplist-{version}.tar.bz2
