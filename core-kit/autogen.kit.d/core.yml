core_dirlisting:
  generator: builtin-dirlisting

  defaults:
    template: ../../templates/simple.tmpl
    files_dir: "{{ .Values.pn }}"
    category: sys-apps

  packages:
    - mime-types:
        category: app-misc
        dir:
          url: https://releases.pagure.org/mailcap/
          matcher: "mailcap-.*.tar.xz$"
        vars:
          homepage: https://pagure.io/mailcap
          desc: Provides /etc/mime.types file
          license: GPL-2
          s: "${WORKDIR}/mailcap-${PV}"
          body: |

            src_install() {
              insinto /etc
              doins mime.types
            }
        transform:
          - kind: string
            match: 'mailcap-'
            replace: ''
          - kind: string
            match: '.tar.xz'
            replace: ''
        assets:
          - name: "mailcap-{{ .Values.version }}.tar.xz"

    - which:
        vars:
          body: |

            src_configure() {
              append-lfs-flags
              tc-export AR
              default
            }

          inherit:
            - flag-o-matic
            - toolchain-funcs
          license: GPL-3
          homepage: https://carlowood.github.io/which/
          desc: Prints out location of specified executables that are in your path
        dir:
          url: https://carlowood.github.io/which/
          matcher: "which-.*.tar.gz$"
        transform:
          - kind: string
            match: 'which-'
            replace: ''
          - kind: string
            match: '.tar.gz'
            replace: ''
        assets:
          - name: "which-{{ .Values.version }}.tar.gz"

    - debianutils:
        vars:
          body: |
            src_prepare() {
              # Avoid adding po4a dependency, upstream refreshes manpages.
              sed -i -e '/SUBDIRS/s|po4a||' Makefile.am || die
              default
              eautoreconf
            }
            src_configure() {
              use static && append-ldflags -static
              default
            }

            src_install() {
              into /
              dobin tempfile run-parts
              if use installkernel ; then
                dosbin installkernel
              fi
              into /usr
              dobin ischroot
              dosbin savelog
              doman ischroot.1 tempfile.1 run-parts.8 savelog.8
              use installkernel && doman installkernel.8
              keepdir /etc/kernel/postinst.d
            }

          inherit:
            - autotools
            - flag-o-matic
          iuse: +installkernel static
          desc: A selection of tools from Debian
          homepage: https://packages.qa.debian.org/d/debianutils.html
          license: BSD GPL-2 SMAIL
        dir:
          url: http://ftp.it.debian.org/debian/pool/main/d/debianutils/
          matcher: "debianutils_.*.tar.xz$"
        transform:
          - kind: string
            match: 'debianutils_'
            replace: ''
          - kind: string
            match: '.tar.xz'
            replace: ''
        assets:
          - name: "debianutils_{{ .Values.version }}.tar.xz"

    - autoconf-archive:
        category: sys-devel
        vars:
          rdepend: |
            !>=gnome-base/gnome-common-3.14.0-r1[-autoconf-archive(+)]
          license: GPL-3
          homepage: https://www.gnu.org/software/autoconf-archive/
          desc: GNU Autoconf Macro Archive
        dir:
          url: http://gnu.mirror.constant.com/autoconf-archive/
          matcher: "autoconf-archive-.*.tar.xz$"
        transform:
          - kind: string
            match: 'autoconf-archive-'
            replace: ''
          - kind: string
            match: '.tar.xz'
            replace: ''
        assets:
          - name: "{{ .Values.pn }}-{{ .Values.version }}.tar.xz"

    - libcap-ng:
        category: sys-libs
        dir:
          url: https://people.redhat.com/sgrubb/libcap-ng/
          matcher: "libcap-ng-.*.tar.gz$"
        transform:
          - kind: string
            match: 'libcap-ng-'
            replace: ''
          - kind: string
            match: '.tar.gz'
            replace: ''
        assets:
          - name: "{{ .Values.pn }}-{{ .Values.version }}.tar.gz"

        vars:
          python_compat: python3+
          inherit:
            - autotools
            - flag-o-matic
            - python-r1
          rdepend: |
            python? ( ${PYTHON_DEPS} )
          depend: |
            ${RDEPEND}
            sys-kernel/linux-headers
            python? ( dev-lang/swig )
          required_use: |
            python? ( ${PYTHON_REQUIRED_USE} )
          iuse: python static-libs
          license: LGPL-2.1
          homepage: https://people.redhat.com/sgrubb/libcap-ng/
          desc: POSIX 1003.1e capabilities
          body: |
            src_prepare() {
              # Apply patch from upstream
              sed -i -e '/^PYTHON = $(PYTHON)/d' bindings/python3/Makefile.am || die
              default
              eautoreconf
            }
            src_configure() {
              use sparc && replace-flags -O? -O0
              local ECONF_SOURCE="${S}"
              local myconf=(
                $(use_enable static-libs static)
                --with-capability_header="${ESYSROOT}"/usr/include/linux/capability.h
              )
              local pythonconf=( --without-python3 )
              if use python ; then
                setup_python_flags_configure() {
                  pythonconf=( --with-python3 )
                  run_in_build_dir econf "${pythonconf[@]}" "${myconf[@]}"
                }
                python_foreach_impl setup_python_flags_configure
              else
                local BUILD_DIR=${WORKDIR}/build
                run_in_build_dir econf "${pythonconf[@]}" "${myconf[@]}"
              fi
            }
            src_compile() {
              if use python; then
                python_foreach_impl run_in_build_dir emake
              else
                local BUILD_DIR=${WORKDIR}/build
                emake -C "${BUILD_DIR}"
              fi
            }
            src_install() {
              if use python; then
                python_foreach_impl run_in_build_dir emake DESTDIR="${D}" install
              else
                local BUILD_DIR=${WORKDIR}/build
                emake -C "${BUILD_DIR}" DESTDIR="${D}" install
              fi
              find "${ED}" -name '*.la' -delete || die
            }


libtools_pkgs:
  generator: builtin-dirlisting

  defaults:
    template: ../../templates/simple.tmpl
    files_dir: "{{ .Values.pn }}"
    category: dev-libs
    assets:
      - name: "libtool-{{ .Values.version }}.tar.xz"

  packages:
    - libltdl:
        vars:
          desc: A shared library tool for developers
          homepage: https://www.gnu.org/software/libtool/
          license: GPL-2
          iuse: static-libs
          s: "${WORKDIR}/libtool-${PV}/libltdl"
          body: |
            src_configure() {
              ECONF_SOURCE=${S}
              econf --enable-ltdl-install \
                $(use_enable static-libs static)
            }
            src_install() {
              emake DESTDIR="${D}" install
            }

        dir:
          url: https://ftp.wayne.edu/gnu/libtool/
          matcher: "libtool-.*.tar.xz$"
        transform:
          - kind: string
            match: 'libtool-'
            replace: ''
          - kind: string
            match: '.tar.xz'
            replace: ''


savannah_sys_apps_http:
  generator: builtin-dirlisting
  defaults:
    template: ../../templates/simple.tmpl
    files_dir: "{{ .Values.pn }}"
    category: sys-apps
    vars:
      license: LGPL-2.1
    assets:
      - name: "{{ .Values.pn }}-{{ .Values.version }}.tar.gz"

  packages:
    - attr:
        vars:
          body: |

            src_prepare() {
              default
              elibtoolize
            }

            src_configure() {
              unset PLATFORM
              export OPTIMIZER=${CFLAGS}
              export DEBUG=-DNDEBUG

              tc-ld-disable-gold

              local myeconfargs=(
                --bindir="${EPREFIX}"/bin
                --libexecdir="${EPREFIX}"/usr/$(get_libdir)
                --enable-shared
                $(use_enable static-libs static)
                $(use_enable nls)
                $(use_enable debug)
              )
              econf "${myeconfargs[@]}"
            }

            src_install() {
              emake DESTDIR="${D}" install

              # we install attr into /bin, so we need the shared lib with it
              gen_usr_ldscript -a attr

              # Add a wrapper until people upgrade.
              insinto /usr/include/attr
              newins "${FILESDIR}"/xattr-shim.h xattr.h

              if ! use static-libs; then
                find "${ED}" -name '*.la' -delete || die
              fi

              einstalldocs
            }

          depend: |
            nls? ( sys-devel/gettext )
          inherit:
            - libtool
            - toolchain-funcs
            - usr-ldscript
          homepage: https://savannah.nongnu.org/projects/attr
          desc: Extended attributes tools
          iuse: debug nls static-libs
        dir:
          url: http://download.savannah.nongnu.org/releases/attr/
          matcher: "attr-.*.tar.gz$"
        transform:
          - kind: string
            match: 'attr-'
            replace: ''
          - kind: string
            match: '.tar.gz'
            replace: ''

    - acl:
        vars:
          body: |

            src_prepare() {
              default
              elibtoolize
            }

            src_configure() {
              local myeconfargs=(
                --bindir="${EPREFIX}"/bin
                --libexecdir="${EPREFIX}"/usr/$(get_libdir)
                $(use_enable static-libs static)
                $(use_enable nls)
              )
              econf "${myeconfargs[@]}"
            }

            src_install() {
              default

              gen_usr_ldscript -a acl

              use static-libs || prune_libtool_files --all
            }

          depend: |
            nls? ( sys-devel/gettext )
          homepage: https://savannah.nongnu.org/projects/acl
          desc: access control list utilities, libraries and headers
          iuse: nls static-libs
          inherit:
            - libtool
            - toolchain-funcs
            - usr-ldscript
            - ltprune
        dir:
          url: http://download.savannah.nongnu.org/releases/acl/
          matcher: "acl-.*.tar.gz$"
        transform:
          - kind: string
            match: 'acl-'
            replace: ''
          - kind: string
            match: '.tar.gz'
            replace: ''

sys_apps_github:
  generator: builtin-github
  defaults:
    files_dir: "{{ .Values.pn }}"
    category: sys-apps
    vars:
      license: LGPL-2.1

  packages:
    - shadow:
        github:
          user: shadow-maint
          query: releases
        vars:
          license: BSD GPL-2
        assets:
          - name: "{{ .Values.pn }}-{{ .Values.version }}.tar.gz"
            matcher: "shadow-.*.tar.gz"


