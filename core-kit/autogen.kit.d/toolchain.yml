sys_devel_gcc:
  generator: builtin-dirlisting

  extensions_defs:
    custom:
      opts:
        script: generators/gcc-math-discover.sh
        mirror: "https://mirrors.kernel.org"

  defaults:
    template: ../../templates/simple.tmpl
    files_dir: '{{ .Values.pn }}'
    category: sys-devel

    vars:
      # use the major version (e.g. 14) as the slot
      slot: '{{- $v := .Values.version  }}{{ join "" ( slice ( splitList "." $v ) 0 1 ) }}'
      inherit:
        - eutils
        - pax-utils
        - toolchain-enable
        - git-r3
      restrict: "strip"
      s: "${WORKDIR}/gcc-${PV}"
      desc: "The GNU Compiler Collection"
      homepage: "https://gcc.gnu.org"
      license: "GPL-3+ LGPL-3+ || ( GPL-3+ libgcc libstdc++ gcc-runtime-library-exception-3.1 ) FDL-1.3+"
      # Slot overridden per-atom for major-version scoping (14, 15, 16)
      keywords: "*"
      iuse: "+cxx d go +fortran objc objc++ objc-gc test doc nls vanilla openmp altivec pch generic_host jit bootstrap bootstrap-lean bootstrap-profiled bootstrap-lto bootstrap-O3 libssp +ssp +pie +vtv link_now ssp_all sanitize dev_extra_warnings +stack_clash_protection checking_no checking_all checking_yes checking_release stage1_checking_no stage1_checking_all stage1_checking_yes stage1_checking_release checking_assert checking_runtime checking_misc checking_tree checking_gc checking_rtlflag checking_df checking_fold checking_gcac checking_rtl checking_valgrind checking_extra stage1_checking_assert stage1_checking_runtime stage1_checking_misc stage1_checking_tree stage1_checking_gc stage1_checking_rtlflag stage1_checking_df stage1_checking_fold stage1_checking_gcac stage1_checking_rtl stage1_checking_valgrind stage1_checking_extra"
      required_use: |
        bootstrap-profiled? ( bootstrap )
        bootstrap-lean? ( bootstrap )
        bootstrap-lto? ( bootstrap )
        bootstrap-O3? ( bootstrap )
        !bootstrap? ( !bootstrap-lean !bootstrap-profiled !bootstrap-lto !bootstrap-O3 )
      cdepend: |
        sys-libs/zlib[static-libs]
        nls? ( sys-devel/gettext[static-libs] )
        virtual/libiconv
        objc-gc? ( >=dev-libs/boehm-gc-7.6[static-libs] )
      depend: |
        ${CDEPEND}
        >=sys-devel/bison-1.875
        >=sys-devel/flex-2.5.4
        >=sys-devel/binutils-2.36.1_p3
        elibc_glibc? ( >=sys-libs/glibc-2.8 )
        test? ( dev-util/dejagnu sys-devel/autogen )
      pdepend: |
        >=sys-devel/gcc-config-1.5
        >=sys-devel/libtool-2.4.3
        dev-cpp/tbb
        elibc_glibc? ( >=sys-libs/glibc-2.8 )


      # GCC checking options (used by gcc_checking_opts helper)
      CHECKS_ALL: 'all'
      CHECKS_YES: 'yes'
      CHECKS_RELEASE: 'release'
      CHECKS_EXTRA: 'extra'
      CHECKS_VALGRIND: 'valgrind'

      # SVN patch reference for backports
      GCC_SVN_PATCH: ''

      patches: []   # Per-version patches overridden in each atom below

      body: |
        CHECKS_ALL="{{ .Values.CHECKS_ALL }}"
        CHECKS_YES="{{ .Values.CHECKS_YES }}"
        CHECKS_RELEASE="{{ .Values.CHECKS_RELEASE }}"
        CHECKS_EXTRA="{{ .Values.CHECKS_EXTRA }}"
        CHECKS_VALGRIND="{{ .Values.CHECKS_VALGRIND }}"
        GCC_SVN_PATCH="{{ .Values.GCC_SVN_PATCH }}"
        GMP_VER="{{ .Values.gmp_ver }}"
        GMP_EXTRAVER="{{ .Values.gmp_extraver }}"
        MPFR_VER="{{ .Values.mpfr_ver }}"
        MPFR_PATCH_VER="{{ .Values.mpfr_patch_ver }}"
        MPC_VER="{{ .Values.mpc_ver }}"
        GCC_A="gcc-${PV%%-r*}.tar.xz"

        pkg_setup() {
          # Capture -march -mcpu and -mtune options to pass to build later.
          MARCH="${MARCH:-$(printf -- "${CFLAGS}" | sed -rne 's/.*-march="?([-_[:alnum:]]+).*/\1/p')}"
          MCPU="${MCPU:-$(printf -- "${CFLAGS}" | sed -rne 's/.*-mcpu="?([-_[:alnum:]]+).*/\1/p')}"
          MTUNE="${MTUNE:-$(printf -- "${CFLAGS}" | sed -rne 's/.*-mtune="?([-_[:alnum:]]+).*/\1/p')}"
          MFPU="${MFPU:-$(printf -- "${CFLAGS}" | sed -rne 's/.*-mfpu="?([-_[:alnum:]]+).*/\1/p')}"
          einfo "Got CFLAGS: ${CFLAGS}"
          einfo "Got GCC_BUILD_CFLAGS: ${GCC_BUILD_CFLAGS}"
          einfo "MARCH: ${MARCH}"
          einfo "MCPU ${MCPU}"
          einfo "MTUNE: ${MTUNE}"
          einfo "MFPU: ${MFPU}"

          # Don't pass cflags/ldflags through.
          unset CFLAGS
          unset CXXFLAGS
          unset CPPFLAGS
          unset LDFLAGS
          unset GCC_SPECS # we don't want to use the installed compiler's specs to build gcc!
          unset LANGUAGES #265283
          export PREFIX=/usr
          CTARGET=${CTARGET:-${CHOST}}
          GCC_BRANCH_VER=${SLOT}
          GCC_CONFIG_VER=${PV}
          DATAPATH=${PREFIX}/share/gcc-data/${CTARGET}/${GCC_CONFIG_VER}
          BINPATH=${PREFIX}/${CTARGET}/gcc-bin/${GCC_CONFIG_VER}

          export CFLAGS="${GCC_BUILD_CFLAGS:--O2 -pipe}"
          export FFLAGS="$CFLAGS"
          export FCFLAGS="$CFLAGS"
          export CXXFLAGS="$CFLAGS"

          LIBPATH=${PREFIX}/lib/gcc/${CTARGET}/${GCC_CONFIG_VER}
          STDCXX_INCDIR=${LIBPATH}/include/g++-v${GCC_BRANCH_VER}

          # Add bootstrap configs to BUILD_CONFIG based on use flags
          if use bootstrap-lto && use bootstrap-lean; then
            BUILD_CONFIG="${BUILD_CONFIG:+${BUILD_CONFIG} }bootstrap-lto-lean"
          elif use bootstrap-lto ; then
            BUILD_CONFIG="${BUILD_CONFIG:+${BUILD_CONFIG} }bootstrap-lto"
          fi
          use bootstrap-O3 && BUILD_CONFIG="${BUILD_CONFIG:+${BUILD_CONFIG} }bootstrap-O3"

          export BUILD_CONFIG

          if [ -n "${GCC_TARGET}" ] ; then
            :
          elif use bootstrap ; then
            if use bootstrap-profiled ; then
              GCC_TARGET="profiledbootstrap"
            else
              GCC_TARGET="bootstrap"
            fi
            # Handle lean bootstrap target suffix (see Makefile.tpl)
            if use bootstrap-lean ; then
              GCC_TARGET="${GCC_TARGET}-lean"
            fi
          else
            GCC_TARGET="all"
          fi
          export GCC_TARGET


          use doc || export MAKEINFO="/dev/null"
        }

        src_unpack() {
          unpack $GCC_A
          ( unpack mpc-{{ .Values.mpc_ver }}.tar.gz && mv ${WORKDIR}/mpc-{{ .Values.mpc_ver }} ${S}/mpc ) || die "mpc setup fail"
          ( unpack mpfr-{{ .Values.mpfr_ver }}.tar.xz && mv ${WORKDIR}/mpfr-{{ .Values.mpfr_ver }} ${S}/mpfr ) || die "mpfr setup fail"
          ( unpack gmp-{{ .Values.gmp_ver }}{{ .Values.gmp_extraver }}.tar.xz && mv ${WORKDIR}/gmp-{{ .Values.gmp_ver }} ${S}/gmp ) || die "gmp setup fail"

          cd "${S}"
          mkdir ${WORKDIR}/objdir

          if use jit; then
            mkdir ${WORKDIR}/objdir-jit
          fi
        }


        src_prepare() {
          # Patch from release to svn branch tip for backports
          if [ -n "${GCC_SVN_PATCH}" ]; then
            eapply "${GCC_SVN_PATCH}" || die "gcc svn patch fail"
          fi
          # For some reason, when upgrading gcc, the gcc Makefile will install stuff

          # like crtbegin.o into a subdirectory based on the name of the currently-installed
          # gcc version, rather than *our* gcc version. Manually fix this:

          sed -i -e "s/^version :=.*/version := ${GCC_CONFIG_VER}/" ${S}/libgcc/Makefile.in || die

          if ! use vanilla; then

            # Prevent libffi from being installed
            sed -i -e 's/\(install.*:\) install-.*recursive/\1/' "${S}"/libffi/Makefile.in || die
            sed -i -e 's/\(install-data-am:\).*/\1/' "${S}"/libffi/include/Makefile.in || die


            # We use --enable-version-specific-libs with ./configure. This
            # option is designed to place all our libraries into a sub-directory
            # rather than /usr/lib*.  However, this option, even through 4.8.0,
            # does not work 100% correctly without a small fix for
            # libgcc_s.so. See: http://gcc.gnu.org/bugzilla/show_bug.cgi?id=32415.
            # So, we apply a small patch to get this working:

            eapply "${FILESDIR}/gcc-4.6.4-fix-libgcc-s-path-with-vsrl.patch" || die "patch fail"

            # Apply Gentoo patches when vanilla is not set
            {{- range $p := .Values.gentoo_patches }}
            eapply "${FILESDIR}/{{ $p }}" || die "gentoo patch {{ $p | base }} failed"
            {{- end }}

            # Harden things up:
            _gcc_prepare_harden
          fi

          # Must be called in src_prepare by EAPI6; applies PATCHES array
          eapply_user
        }

        _gcc_prepare_harden() {
          local gcc_hard_flags=""

          # Selectively enable features from hardening patches
          use ssp_all && gcc_hard_flags+=" -DENABLE_DEFAULT_SSP_ALL"
          use link_now && gcc_hard_flags+=" -DENABLE_DEFAULT_LINK_NOW"
          use stack_clash_protection && gcc_hard_flags+=" -DENABLE_DEFAULT_SCP"

          sed -e '/^ALL_CFLAGS/iHARD_CFLAGS = ' \
            -e 's|^ALL_CFLAGS = |ALL_CFLAGS = $(HARD_CFLAGS) |' \
            -i "${S}"/gcc/Makefile.in

          sed -e '/^ALL_CXXFLAGS/iHARD_CFLAGS = ' \
            -e 's|^ALL_CXXFLAGS = |ALL_CXXFLAGS = $(HARD_CFLAGS) |' \
            -i "${S}"/gcc/Makefile.in

          sed -i -e "/^HARD_CFLAGS = /s|=|= ${gcc_hard_flags} |" "${S}"/gcc/Makefile.in || die
        }

        gcc_conf_lang_opts() {
          # Determine language support:
          local conf_gcc_lang=""
          local GCC_LANG="c,c++"
          if use objc; then
            GCC_LANG+=",objc"
            use objc-gc && conf_gcc_lang+=" --enable-objc-gc"
            use objc++ && GCC_LANG+=",obj-c++"
          fi

          use fortran && GCC_LANG+=",fortran" || conf_gcc_lang+=" --disable-libquadmath"

          use go && GCC_LANG+=",go"

          use d && GCC_LANG+=",d"

          conf_gcc_lang+=" --enable-languages=${GCC_LANG} --disable-libgcj"

          printf -- "${conf_gcc_lang}"
        }

        # ARM
        gcc_conf_arm_opts() {
          # Skip the rest if not an arm target
          [[ ${CTARGET} == arm* ]] || return

          local conf_gcc_arm=""
          local arm_arch=${CTARGET%%-*}
          local a
          # Remove trailing endian variations first: eb el be bl b l
          for a in e{b,l} {b,l}e b l ; do
            if [[ ${arm_arch} == *${a} ]] ; then
              arm_arch=${arm_arch%${a}}
              break
            fi
          done

          # Convert armv7{a,r,m} to armv7-{a,r,m}
          [[ ${arm_arch} == armv7? ]] && arm_arch=${arm_arch/7/7-}

          # See if this is a valid --with-arch flag
          if (srcdir=${S}/gcc target=${CTARGET} with_arch=${arm_arch};
            . "${srcdir}"/config.gcc) &>/dev/null
          then
            conf_gcc_arm+=" --with-arch=${arm_arch}"
          fi

          # Enable hardvfp
          local float="hard"
          local default_fpu=""

          case "${CTARGET}" in
            *[-_]softfloat[-_]*) float="soft" ;;
            *[-_]softfp[-_]*) float="softfp" ;;
            armv[56]*) default_fpu="vfpv2" ;;
            armv7ve*) default_fpu="vfpv4-d16" ;;
            armv7*) default_fpu="vfpv3-d16" ;;
            amrv8*) default_fpu="fp-armv8" ;;
          esac

          conf_gcc_arm+=" --with-float=$float"
          [ -z "${MFPU}" ] && [ -n "${default_fpu}" ] && conf_gcc_arm+=" --with-fpu=${default_fpu}"

          printf -- "${conf_gcc_arm}"
        }

        src_configure() {

          local confgcc
          confgcc+=" --enable-threads=posix --enable-__cxa_atexit --enable-libstdcxx-time"
          confgcc+=" $(use_enable openmp libgomp)"
          confgcc+=" $(use_enable bootstrap) --enable-shared"

          [[ -n ${CBUILD} ]] && confgcc+=" --build=${CBUILD}"

          confgcc+=" $(use_enable sanitize libsanitizer)"
          confgcc+=" $(use_enable pie default-pie)"
          confgcc+=" $(use_enable ssp default-ssp)"
          ! use pch && confgcc+=" --disable-libstdcxx-pch"
          use vtv && confgcc+=" --enable-vtable-verify --enable-libvtv"
          ! use vtv && confgcc+=" --disable-vtable-verify --disable-libvtv"

          use libssp || export gcc_cv_libc_provides_ssp=yes

          local branding="MacaroniOS ${PVR}"

          confgcc+=" --with-python-dir=${DATAPATH/$PREFIX/}/python"
          use nls && confgcc+=" --enable-nls --with-included-gettext" || confgcc+=" --disable-nls"

          use generic_host || confgcc+="${MARCH:+ --with-arch=${MARCH}}${MCPU:+ --with-cpu=${MCPU}}${MTUNE:+ --with-tune=${MTUNE}}${MFPU:+ --with-fpu=${MFPU}}"
          P= cd ${WORKDIR}/objdir && ../gcc-${PV}/configure \
            ${BUILD_CONFIG:+--with-build-config="${BUILD_CONFIG}"} \
            $(use_enable libssp) \
            --disable-multilib \
            --enable-version-specific-runtime-libs \
            --prefix=${PREFIX} \
            --bindir=${BINPATH} \
            --includedir=${LIBPATH}/include \
            --datadir=${DATAPATH} \
            --mandir=${DATAPATH}/man \
            --infodir=${DATAPATH}/info \
            --with-gxx-include-dir=${STDCXX_INCDIR} \
            --enable-clocale=gnu \
            --host=$CHOST \
            --enable-obsolete \
            --disable-werror \
            --enable-libmudflap \
            --enable-secureplt \
            --enable-lto \
            --with-system-zlib \
            --with-bugurl=https://github.com/macaroni-os/mark-issues/issues \
            --with-pkgversion="$branding" \
            $(gcc_checking_opts stage1) $(gcc_checking_opts) \
            $(gcc_conf_lang_opts) $(gcc_conf_arm_opts) $confgcc \
            || die "configure fail"

          if use jit; then
            P= cd ${WORKDIR}/objdir-jit && ../gcc-${PV}/configure \
                ${BUILD_CONFIG:+--with-build-config="${BUILD_CONFIG}"} \
                $(use_enable libssp) \
                --disable-multilib \
                --enable-version-specific-runtime-libs \
                --prefix=${PREFIX} \
                --bindir=${BINPATH} \
                --includedir=${LIBPATH}/include \
                --datadir=${DATAPATH} \
                --mandir=${DATAPATH}/man \
                --infodir=${DATAPATH}/info \
                --with-gxx-include-dir=${STDCXX_INCDIR} \
                --enable-clocale=gnu \
                --host=$CHOST \
                --enable-obsolete \
                --disable-werror \
                --enable-libmudflap \
                --enable-secureplt \
                --enable-lto \
                --with-system-zlib \
                --with-bugurl=https://github.com/macaroni-os/mark-issues/issues \
                --with-pkgversion="$branding" \
                $(gcc_checking_opts stage1) $(gcc_checking_opts) \
                --enable-languages=jit --enable-host-shared --with-pic \
                $(gcc_conf_arm_opts) $confgcc \
                || die "configure fail"
          fi
        }

        src_compile() {
          cd $WORKDIR/objdir
          unset ABI
          if use bootstrap || use bootstrap-profiled || use bootstrap-lean; then
            emake BOOT_CFLAGS="${CFLAGS}" P= LIBPATH="${LIBPATH}" ${GCC_TARGET} || die "compile fail"
          else
            emake P= LIBPATH="${LIBPATH}" ${GCC_TARGET} || die "compile fail"
          fi
          if use jit; then
            cd $WORKDIR/objdir-jit
            unset ABI
            emake P= LIBPATH="${LIBPATH}" ${GCC_TARGET} || die "compile fail"
          fi
        }

        src_test() {
          cd "${WORKDIR}/objdir"
          unset ABI
          local tests_failed=0
          ( ulimit -s 65536 && ${MAKE:-make} ${MAKEOPTS} LIBPATH="${ED%/}/${LIBPATH}" -k check RUNTESTFLAGS="-v -v -v" 2>&1 | tee ${T}/make-check-log ) || tests_failed=1
          "../${S##*/}/contrib/test_summary" 2>&1 | tee "${T}/gcc-test-summary.out"
          [ ${tests_failed} -eq 0 ] || die "make -k check failed"
        }

        create_gcc_env_entry() {
          dodir /etc/env.d/gcc
          local gcc_envd_base="/etc/env.d/gcc/${CTARGET}-${GCC_CONFIG_VER}"
          local gcc_envd_file="${D}${gcc_envd_base}"
          if [ -z "$1" ]; then
            gcc_specs_file=""
          else
            gcc_envd_file="$gcc_envd_file-$1"
            gcc_specs_file="${LIBPATH}/$1.specs"
          fi
          cat <<-EOF > ${gcc_envd_file}
          GCC_PATH="${BINPATH}"
          LDPATH="${LIBPATH}:${LIBPATH}/32"
          MANPATH="${DATAPATH}/man"
          INFOPATH="${DATAPATH}/info"
          STDCXX_INCDIR="${STDCXX_INCDIR##*/}"
          GCC_SPECS="${gcc_specs_file}"
          EOF
        }

        linkify_compiler_binaries() {
          dodir ${PREFIX}/bin
          cd "${D}"${BINPATH}
          # Ugh: we really need to auto-detect this list.
          #       It's constantly out of date.

          local binary_languages="cpp gcc g++ c++ gcov"
          local gnat_bins="gnat gnatbind gnatchop gnatclean gnatfind gnatkr gnatlink gnatls gnatmake gnatname gnatprep gnatxref"

          use go && binary_languages="${binary_languages} gccgo"
          use fortran && binary_languages="${binary_languages} gfortran"
          use d && binary_languages="${binary_languages} gdc"

          for x in ${binary_languages} ; do
            [[ -f ${x} ]] && mv ${x} ${CTARGET}-${x}

            if [[ -f ${CTARGET}-${x} ]] ; then
              ln -sf ${CTARGET}-${x} ${x}
              dosym ${BINPATH}/${CTARGET}-${x} ${PREFIX}/bin/${x}-${GCC_CONFIG_VER}
              # Create version-ed symlinks
              dosym ${BINPATH}/${CTARGET}-${x} ${PREFIX}/bin/${CTARGET}-${x}-${GCC_CONFIG_VER}
            fi

            if [[ -f ${CTARGET}-${x}-${GCC_CONFIG_VER} ]] ; then
              rm -f ${CTARGET}-${x}-${GCC_CONFIG_VER}
              ln -sf ${CTARGET}-${x} ${CTARGET}-${x}-${GCC_CONFIG_VER}
            fi
          done
        }

        tasteful_stripping() {
          # Now do the fun stripping stuff
          env RESTRICT="" CHOST=${CHOST} dostrip "${D}${BINPATH}" ; \
          env RESTRICT="" CHOST=${CTARGET} dostrip "${D}${LIBPATH}"
          # gcc used to install helper binaries in lib/ but then moved to libexec/
          [[ -d ${D}${PREFIX}/libexec/gcc ]] && \
            env RESTRICT="" CHOST=${CHOST} dostrip "${D}${PREFIX}/libexec/gcc/${CTARGET}/${GCC_CONFIG_VER}"
        }

        doc_cleanups() {
          local cxx_mandir=$(find "${WORKDIR}/objdir/${CTARGET}/libstdc++-v3" -name man)
          if [[ -d ${cxx_mandir} ]] ; then
            # clean bogus manpages #113902
            find "${cxx_mandir}" -name '*_build_*' -exec rm {} \;
            ( set +f ; cp -r "${cxx_mandir}"/man? "${D}/${DATAPATH}"/man/ )
          fi

          # Remove info files if we don't want them.
          if ! use doc || has noinfo ${FEATURES} ; then
            rm -r "${D}/${DATAPATH}"/info
          else
            prepinfo "${DATAPATH}"
          fi

          # Strip man files too if 'noman' feature is set.
          if has noman ${FEATURES} ; then
            rm -r "${D}/${DATAPATH}"/man
          else
            prepman "${DATAPATH}"
          fi
        }

        src_install() {
          if use jit; then
            S=$WORKDIR/objdir-jit; cd "${S}"
            make -j1 DESTDIR="${D}" install || die
            mv ${D}${PREFIX}/lib/libgccjit.so* ${D}${LIBPATH}/
          fi

          S=$WORKDIR/objdir; cd "${S}"

        # PRE-MAKE INSTALL SECTION:

          # Don't allow symlinks in private gcc include dir as this can break the build
          ( set +f ; find gcc/include*/ -type l -delete 2>/dev/null )

          # Remove generated headers, as they can cause things to break
          # (ncurses, openssl, etc).
          while read x; do
            grep -q 'It has been auto-edited by fixincludes from' "${x}" \
              && echo "Removing auto-generated header: $x" \
              && rm -f "${x}"
          done < <(find gcc/include*/ -name '*.h')

        # MAKE INSTALL SECTION:

          make -j1 DESTDIR="${D}" install || die

        # POST MAKE INSTALL SECTION:
          # Basic sanity check
          local EXEEXT
          eval $(grep ^EXEEXT= "${WORKDIR}"/objdir/gcc/config.log)
          [[ -r ${D}${BINPATH}/gcc${EXEEXT} ]] || die "gcc not found in ${D}"

          # Install compat wrappers
          exeinto "${DATAPATH}"
          ( set +f ; doexe "${FILESDIR}"/c{89,99} || die )

          # Setup env.d entry
          dodir /etc/env.d/gcc
          create_gcc_env_entry

        # CLEANUPS:

          # Punt some tools which are really only useful while building gcc
          find "${D}" -name install-tools -prune -type d -exec rm -rf "{}" \; 2>/dev/null
          # This one comes with binutils
          find "${D}" -name libiberty.a -delete 2>/dev/null
          # prune empty dirs left behind
          find "${D}" -depth -type d -delete 2>/dev/null
          # ownership fix:
          chown -R root:0 "${D}"${LIBPATH} 2>/dev/null

          linkify_compiler_binaries
          tasteful_stripping

          # Remove python files in the lib path
          find "${D}/${LIBPATH}" -name "*.py" -type f -exec rm "{}" \; 2>/dev/null

          # Remove unwanted docs and prepare the rest for installation
          doc_cleanups

          # Cleanup undesired libtool archives
          find "${D}" \
            '(' \
              -name 'libstdc++.la' -o -name 'libstdc++fs.la' -o -name 'libsupc++.la' -o \
              -name 'libcc1.la' -o -name 'libcc1plugin.la' -o -name 'libcp1plugin.la' -o \
              -name 'libgomp.la' -o -name 'libgomp-plugin-*.la' -o \
              -name 'libgfortran.la' -o -name 'libgfortranbegin.la' -o \
              -name 'libmpx.la' -o -name 'libmpxwrappers.la' -o \
              -name 'libitm.la' -o -name 'libvtv.la' -o -name 'lib*san.la' \
            ')' -type f -delete 2>/dev/null

          # replace gcc_movelibs - currently handles only libcc1:
          ( set +f
            einfo -- "Removing extraneous libtool '.la' files from '${PREFIX}/lib*'."
            rm ${D%/}${PREFIX}/lib{,32,64}/*.la 2>/dev/null
            einfo -- "Relocating libs to '${LIBPATH}':"
            for l in "${D%/}${PREFIX}"/lib{,32,64}/* ; do
              [ -f "${l}" ] || continue
              mydir="${l%/*}" ; myfile="${l##*/}"
              einfo -- "Moving '${myfile}' from '${mydir#${D}}' to '${LIBPATH}'."
              cd "${mydir}" && mv "${myfile}" "${D}${LIBPATH}/${myfile}" 2>/dev/null || die
            done
          )

          # the .la files that are installed have weird embedded single quotes around abs
          # paths on the dependency_libs line. The following code finds and fixes them:

          for x in $(find ${D}${LIBPATH} -iname '*.la'); do
            dep="$(grep ^dependency_libs $x)"
            [ "$dep" == "" ] && continue
            inner_dep="${dep#dependency_libs=}"
            inner_dep="${inner_dep//\'/}"
            inner_dep="${inner_dep# *}"
            sed -i -e "s:^dependency_libs=.*$:dependency_libs=\'${inner_dep}\':g" $x || die
          done

          # Don't scan .gox files for executable stacks - false positives
          if use go; then
            export QA_EXECSTACK="${PREFIX#/}/lib*/go/*/*.gox"
            export QA_WX_LOAD="${PREFIX#/}/lib*/go/*/*.gox"
          fi

          # Disable RANDMMAP so PCH works.
          pax-mark -r "${D}${PREFIX}/libexec/gcc/${CTARGET}/${GCC_CONFIG_VER}/cc1" ; \
          pax-mark -r "${D}${PREFIX}/libexec/gcc/${CTARGET}/${GCC_CONFIG_VER}/cc1plus"
        }

        pkg_postinst() {
          # hack from gentoo - should probably be handled better:
          ( set +f ; cp "${ROOT}${DATAPATH}"/c{89,99} "${ROOT}${PREFIX}/bin/" 2>/dev/null )

          PATH="${BINPATH}:${PATH}"
          export PATH
          compiler_auto_enable ${PV} ${CTARGET}
        }


        # GCC internal self checking options
        # Usage: gcc_checking_opts [stage1]
        gcc_checking_opts() {
          local stage1="${1}${1:+_}"

          local opts check checks
          # Setting checking_no overrides all other checks
          if use ${stage1}checking_no ; then
            opts="no"
          else
            # Priority is all > yes > release
            if use ${stage1}checking_all ; then
              checks="${CHECKS_ALL}"
            elif use ${stage1}checking_yes ; then
              checks="${CHECKS_YES}"
            elif use ${stage1}checking_release ; then
              checks="${CHECKS_RELEASE}"
            fi

            # Check if explict use flags are set for any valid checks
            for check in ${CHECKS_ALL} ${CHECKS_VALGRIND} ; do
              # Check if the flag is enabled and add to list if not there; force extra to set the same for both scopes.
              if use ${stage1}checking_${check} || ( [ -n "${CHECKS_EXTRA}" ] && [ "${check}" = "extra" ] && ( use stage1_checking_extra || use checking_extra ) ) ; then
                has check "${checks}" || checks="${checks} ${check}"
              fi
            done

            # If no checking has been defined, set defaults
            if [ -z "${checks}" ] ; then
              if [ -n "${stage1}" ] ; then
                checks="${CHECKS_YES}"
              else
                checks="${CHECKS_RELEASE}"
              fi
            fi

            # build our opts string
            for check in ${checks} ; do
              opts="${opts}${opts:+,}${check}"
            done
          fi


          printf -- "--enable-${stage1:+${stage1%_}-}checking=${opts}"
        }


  packages:
    - gcc:
        dir:
          url: https://mirrors.kernel.org/gnu/gcc/
          matcher: 'gcc-[0-9]+\.[0-9]+\.[0-9]+'
        selector:
          - '>14'
          - '<15'
        assets:
          - name: 'gcc-{{ .Values.version }}.tar.xz'
            url: 'https://mirrors.kernel.org/gnu/gcc/gcc-{{ .Values.version }}/gcc-{{ .Values.version }}.tar.xz'
        transform:
          - kind: string
            match: 'gcc-'
            replace: ''
        extensions: [custom]

        vars:
          gentoo_patches:
            - gentoo-patches/gcc-14.3.0/01_all_default-fortify-source.patch
            - gentoo-patches/gcc-14.3.0/02_all_default-warn-format-security.patch
            - gentoo-patches/gcc-14.3.0/03_all_default-warn-trampolines.patch
            - gentoo-patches/gcc-14.3.0/04_all_nossp-on-nostdlib.patch
            - gentoo-patches/gcc-14.3.0/05_all_alpha-mieee-default.patch
            - gentoo-patches/gcc-14.3.0/06_all_ia64_note.GNU-stack.patch
            - gentoo-patches/gcc-14.3.0/07_all_libiberty-asprintf.patch
            - gentoo-patches/gcc-14.3.0/08_all_libiberty-pic.patch
            - gentoo-patches/gcc-14.3.0/09_all_esysroot.patch
            - gentoo-patches/gcc-14.3.0/10_all_sh-drop-sysroot-suffix.patch
            - gentoo-patches/gcc-14.3.0/11_all_ia64-TEXTREL.patch
            - gentoo-patches/gcc-14.3.0/14_all_respect-build-cxxflags.patch
            - gentoo-patches/gcc-14.3.0/15_all_DEF_GENTOO_GLIBCXX_ASSERTIONS.patch
            - gentoo-patches/gcc-14.3.0/20_all_libstdcxx-no-vtv.patch
            - gentoo-patches/gcc-14.3.0/22_all_default_ssp-buffer-size.patch
            - gentoo-patches/gcc-14.3.0/23_all_DEF_GENTOO_ZNOW-z-now.patch
            - gentoo-patches/gcc-14.3.0/24_all_DEF_GENTOO_SCP-fstack-clash-protection.patch
            # CET already enabled by default in gcc-14.4+
#            - gentoo-patches/gcc-14.3.0/26_all_enable-cet.patch
            - gentoo-patches/gcc-14.3.0/28_all_drop_CFLAGS_sed.patch
            - gentoo-patches/gcc-14.3.0/29_all_msgfmt-libstdc++-link.patch
            - gentoo-patches/gcc-14.3.0/30_all_tar_libstdc++-link.patch
            # __TIME64_DST_ already present in gcc-14.4+
#            - gentoo-patches/gcc-14.3.0/31_all_time64.patch
            # libcody/client.cc changed in gcc-14.4, patch doesn't apply
#            - gentoo-patches/gcc-14.3.0/83_all_PR122941-cxx20.patch
            - gentoo-patches/gcc-14.3.0/32_all_time64_ssemath.patch
            - gentoo-patches/gcc-14.3.0/70_all_PR117854-config-nvptx-fix-bashisms-with-gen-copyright.sh-use.patch
            - gentoo-patches/gcc-14.3.0/79_all_sanitizer_time64.patch
            - gentoo-patches/gcc-14.3.0/80_all_genoutput-Accelerate-the-place_operands-function.patch
            - gentoo-patches/gcc-14.3.0/81_all_genemit-Distribute-evenly-to-files-PR111600.patch
            - gentoo-patches/gcc-14.3.0/82_all_genrecog-Split-into-separate-partitions-PR111600.patch
    - gcc:
        dir:
          url: https://mirrors.kernel.org/gnu/gcc/
          matcher: 'gcc-[0-9]+\.[0-9]+\.[0-9]+'
        selector:
          - '>15'
          - '<16'
        assets:
          - name: 'gcc-{{ .Values.version }}.tar.xz'
            url: 'https://mirrors.kernel.org/gnu/gcc/gcc-{{ .Values.version }}/gcc-{{ .Values.version }}.tar.xz'
        transform:
          - kind: string
            match: 'gcc-'
            replace: ''
        extensions: [custom]

        vars:
          gentoo_patches:
            - gentoo-patches/gcc-15.2.0/01_all_default-fortify-source.patch
            - gentoo-patches/gcc-15.2.0/02_all_default-warn-format-security.patch
            - gentoo-patches/gcc-15.2.0/03_all_default-warn-trampolines.patch
            - gentoo-patches/gcc-15.2.0/04_all_nossp-on-nostdlib.patch
            - gentoo-patches/gcc-15.2.0/05_all_alpha-mieee-default.patch
            - gentoo-patches/gcc-15.2.0/06_all_ia64_note.GNU-stack.patch
            - gentoo-patches/gcc-15.2.0/08_all_libiberty-pic.patch
            - gentoo-patches/gcc-15.2.0/09_all_esysroot.patch
            - gentoo-patches/gcc-15.2.0/10_all_sh-drop-sysroot-suffix.patch
            - gentoo-patches/gcc-15.2.0/11_all_ia64-TEXTREL.patch
            - gentoo-patches/gcc-15.2.0/14_all_respect-build-cxxflags.patch
            - gentoo-patches/gcc-15.2.0/15_all_DEF_GENTOO_GLIBCXX_ASSERTIONS.patch
            - gentoo-patches/gcc-15.2.0/20_all_libstdcxx-no-vtv.patch
            - gentoo-patches/gcc-15.2.0/22_all_default_ssp-buffer-size.patch
            - gentoo-patches/gcc-15.2.0/23_all_DEF_GENTOO_ZNOW-z-now.patch
            - gentoo-patches/gcc-15.2.0/24_all_DEF_GENTOO_SCP-fstack-clash-protection.patch
            - gentoo-patches/gcc-15.2.0/26_all_enable-cet.patch
            - gentoo-patches/gcc-15.2.0/28_all_drop_CFLAGS_sed.patch
            - gentoo-patches/gcc-15.2.0/29_all_msgfmt-libstdc++-link.patch
            - gentoo-patches/gcc-15.2.0/30_all_tar_libstdc++-link.patch
            - gentoo-patches/gcc-15.2.0/31_all_testsuite-fortran-pass-Wcomplain-wrong-lang-where-ap.patch
            - gentoo-patches/gcc-15.2.0/32_all_time64.patch
            - gentoo-patches/gcc-15.2.0/33_all_PR80677-cross-limits.patch
            - gentoo-patches/gcc-15.2.0/34_all_time64_ssemath.patch
            - gentoo-patches/gcc-15.2.0/35_all_checking-gc-use-heuristics.patch
            - gentoo-patches/gcc-15.2.0/76_all_PR117854-config-nvptx-fix-bashisms-with-gen-copyright.sh-use.patch
            - gentoo-patches/gcc-15.2.0/84_all_PR116975-GDCFLAGS.patch

    - gcc:
        dir:
          url: https://mirrors.kernel.org/gnu/gcc/
          matcher: 'gcc-[0-9]+\.[0-9]+\.[0-9]+'
        selector:
          - '>16'
          - '<17'
        assets:
          - name: 'gcc-{{ .Values.version }}.tar.xz'
            url: 'https://mirrors.kernel.org/gnu/gcc/gcc-{{ .Values.version }}/gcc-{{ .Values.version }}.tar.xz'
        transform:
          - kind: string
            match: 'gcc-'
            replace: ''
        extensions: [custom]

        vars:
          gentoo_patches:
            - gentoo-patches/gcc-16.2.0/01_all_default-fortify-source.patch
            - gentoo-patches/gcc-16.2.0/02_all_default-warn-format-security.patch
            - gentoo-patches/gcc-16.2.0/03_all_default-warn-trampolines.patch
            - gentoo-patches/gcc-16.2.0/04_all_nossp-on-nostdlib.patch
            - gentoo-patches/gcc-16.2.0/05_all_alpha-mieee-default.patch
            - gentoo-patches/gcc-16.2.0/06_all_ia64_note.GNU-stack.patch
            - gentoo-patches/gcc-16.2.0/08_all_libiberty-pic.patch
            - gentoo-patches/gcc-16.2.0/09_all_esysroot.patch
            - gentoo-patches/gcc-16.2.0/10_all_sh-drop-sysroot-suffix.patch
            - gentoo-patches/gcc-16.2.0/11_all_ia64-TEXTREL.patch
            - gentoo-patches/gcc-16.2.0/14_all_respect-build-cxxflags.patch
            - gentoo-patches/gcc-16.2.0/15_all_DEF_GENTOO_GLIBCXX_ASSERTIONS.patch
            - gentoo-patches/gcc-16.2.0/20_all_libstdcxx-no-vtv.patch
            - gentoo-patches/gcc-16.2.0/22_all_default_ssp-buffer-size.patch
            - gentoo-patches/gcc-16.2.0/23_all_DEF_GENTOO_ZNOW-z-now.patch
            - gentoo-patches/gcc-16.2.0/24_all_DEF_GENTOO_SCP-fstack-clash-protection.patch
            - gentoo-patches/gcc-16.2.0/26_all_enable-cet.patch
            - gentoo-patches/gcc-16.2.0/28_all_drop_CFLAGS_sed.patch
            - gentoo-patches/gcc-16.2.0/29_all_msgfmt-libstdc++-link.patch
            - gentoo-patches/gcc-16.2.0/30_all_tar_libstdc++-link.patch
            - gentoo-patches/gcc-16.2.0/32_all_time64.patch
            - gentoo-patches/gcc-16.2.0/33_all_PR80677-cross-limits.patch
            - gentoo-patches/gcc-16.2.0/34_all_time64_ssemath.patch
            - gentoo-patches/gcc-16.2.0/35_all_checking-gc-use-heuristics.patch
            - gentoo-patches/gcc-16.2.0/36_all_no-afdo-testing.patch
            - gentoo-patches/gcc-16.2.0/37_all_PR66487-object-lifetime-instrumentation-for-Valgrind.patch
            - gentoo-patches/gcc-16.2.0/38_all_hurd_linker_path.patch
            - gentoo-patches/gcc-16.2.0/76_all_PR117854-config-nvptx-fix-bashisms-with-gen-copyright.sh-use.patch
            - gentoo-patches/gcc-16.2.0/86_all_PR122794-libtool.patch
