zfs_and_friends:
  generator: github-1
  defaults:
    kernel_compat: 6.4
    github:
      user: openzfs
      repo: zfs
      query: tags
      match: '^zfs-([0-9.]+)$'
  packages:
    - zfs
    - zfs-kmod

