# Config file for /etc/init.d/caddy

# The user to run the Caddy as
CADDY_USER=caddy

# The group to run the Caddy as
CADDY_GROUP=caddy

# Location of default Caddy config file
CADDY_CONFIG="/etc/caddy/Caddyfile"

# Extra args to pass Caddy
CADDY_ARGS="run --config /etc/caddy/Caddyfile"
