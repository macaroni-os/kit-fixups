apache_tools:
  generator: dirlisting-1
  packages:
    - apache-tools:
        cat: app-admin
        dir:
          url: https://archive.apache.org/dist/httpd/
          format: tar.bz2
          order: asc
          files:
            - httpd
