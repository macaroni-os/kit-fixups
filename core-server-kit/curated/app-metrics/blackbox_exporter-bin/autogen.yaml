blackbox_exporter-bin:
  generator: github-1
  packages:
    - blackbox_exporter-bin:
        tarball: blackbox_exporter-{version}.linux-amd64.tar.gz
        github:
          user: prometheus
          repo: blackbox_exporter
          query: releases
