# Config file for /etc/init.d/prometheus

# The user to run the Prometheus as
PROMETHEUS_USER=prometheus

# The group to run the Prometheus as
PROMETHEUS_GROUP=prometheus

# Location of default Prometheus config file
PROMETHEUS_CONFIG="/etc/prometheus/prometheus.yml"

# Extra args to pass Prometheus
PROMETHEUS_ARGS="--config.file=/etc/prometheus/prometheus.yml --storage.tsdb.path=/var/lib/prometheus/data --web.enable-lifecycle"
