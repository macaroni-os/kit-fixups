memcached_exporter-bin:
  generator: github-1
  packages:
    - memcached_exporter-bin:
        select: 'v\d+\.\d+\.\d+$'
        tarball: memcached_exporter-{version}.linux-amd64.tar.gz
        github:
          user: prometheus
          repo: memcached_exporter
          query: releases
