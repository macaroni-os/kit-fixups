mysqld_exporter-bin:
  generator: github-1
  packages:
    - mysqld_exporter-bin:
        select: 'v\d+\.\d+\.\d+$'
        tarball: mysqld_exporter-{version}.linux-amd64.tar.gz
        github:
          user: prometheus
          repo: mysqld_exporter
          query: releases
