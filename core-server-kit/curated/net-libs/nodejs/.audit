This is a copy of gentoo's nodejs directory, but it's from 1.4-release era, and put into curated
so that it also applies to 1.3-release as it is required since the nodejs in 1.3-release
(snapshotted from gentoo) had some weird openssl deps (like depending on = version rather than
>= version).

