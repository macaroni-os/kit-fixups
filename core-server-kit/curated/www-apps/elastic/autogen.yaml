elastic_rules:
  defaults:
    github_user: elastic
  packages:
    - kibana-bin:
        cat: www-apps
        nodejs: True
    - elasticsearch:
        cat: app-misc
    - logstash-bin:
        cat: app-admin
