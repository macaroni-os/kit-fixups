timeshift:
  generator: github-1
  defaults:
    select: 'v\d+\.\d+\.\d+$'
  packages:
    - timeshift:
        github:
          user: teejee2008
          repo: timeshift
          query: releases

