dev_python_autogen_rule:
  defaults:
    cat: dev-vcs
    python_compat: python3+
  generator: pypi-1
  packages:
    - mercurial

protobuf_stuff:
  defaults:
    cat: dev-python
    python_compat: python3+
  generator: pypi-simple-1
  packages:
    - protobuf-python:
        
