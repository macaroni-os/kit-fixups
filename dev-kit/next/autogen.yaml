devlibs_github:
  defaults:
    cat: dev-libs/
  generator: github-1
  packages:
    - libmaxminddb:
        desc: C library for the MaxMind DB file format.
        homepage: https://maxmind.github.io/libmaxminddb/
        github:
          user: maxmind
          repo: libmaxminddb
          query: releases
