inkscape:
  generator: inkscape
  packages:
    - inkscape:
        version: '1.3.2'

lockfile:
  defaults:
    cat: dev-python
    python_compat: python3+
  generator: pypi-simple-1
  packages:
    - lockfile:
        pydeps:
          py:3:build:
            - setuptools
