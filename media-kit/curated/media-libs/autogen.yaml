media_catpkg:
  generator: github
  defaults:
    cat: media-libs
  packages:
    - libjpeg-turbo:
        github:
          query: releases
    - libheif:
        github:
          user: strukturag
          query: releases
        tarball: libheif-{version}.tar.gz
