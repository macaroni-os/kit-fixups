libmypaint:
  generator: github-1
  packages:
    - libmypaint:
        python_compat: "python3+"
        tarball: libmypaint-{version}.tar.xz
        github:
          user: mypaint
          repo: libmypaint
          query: releases
