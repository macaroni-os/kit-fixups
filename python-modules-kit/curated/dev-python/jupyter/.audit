Per FL-5275: jupyter require tornado-4* at runtime. This is appears not tracked in gentoo ebuild. Keep the fix in here. Upstream has the work done on migarting to the tornado-5. This will require bump for the jupyter stack (it is a bunch of subprojects).
https://github.com/jupyter/notebook/pull/3034
