osgeo_proj4_rules:
  generator: github-1
  defaults:
    github:
      user: OSGeo
      query: releases

  packages:
    - proj:
        github:
          repo: PROJ
    - proj-data:
        github:
          repo: PROJ-data
